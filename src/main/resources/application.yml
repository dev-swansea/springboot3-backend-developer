server:
  port: 8888

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/suwan_board?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: board
    password: board
  jpa:
    show-sql: true # 전송 쿼리 확인
    properties:
      hibernate:
        format_sql: true
      ddl-auto: create
    hibernate:
      defer-datasource-initialization: true
  sql:
    init:
      mode: always